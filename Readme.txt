A simple client for twitter written in dojo
To run first do 'npm install' , to install oauth npm package
and then 'node server' and open http://localhost:8000 on your browser
Naming conventions
- template files are in lowercase
- for widget files , Widget is suffixed at the end
